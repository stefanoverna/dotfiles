#!/bin/sh

REDISPID='/usr/local/var/run/redis.pid'

case "$1" in
  start)
    redis-server /usr/local/etc/redis.conf
    ;;
  stop)
    PID=`cat $REDISPID`
    if [ ! -z "$PID" ]; then
      kill -15 $PID
    fi
    ;;
  *)
    redis-server $@
esac

