#!/bin/bash

DEFAULT_PANE_FILE=".plumber-pane"
PANE_FILE="${1:-$DEFAULT_PANE_FILE}"

if [ ! -p $PIPE_FILE ]; then
  exit
fi

PANE_NAME=$(cat $PANE_FILE)

# Exit copy mode
tmux send-keys -t $PANE_NAME $'\e'

# Clear screen
tmux send-keys -t $PANE_NAME -R
tmux clear-history -t $PANE_NAME

